---
import type { Cite } from "../types.ts"

interface Props {
    Citations: Cite[],
}

const { Citations } = Astro.props;
---

<div class="u-container py-3">
    <ul class="Container">
        {
            Citations.map((c) => (
                <li class="cite-list transition duration-1000 ease-in target:animate-cite" id={c.Inline}>
                    <h4 >{"[" + c.Inline + "]"}</h4>
                    <p class="Citation">{c.mla}</p>
                </li>
            ))
        }
    </ul>
</div>

<style>
    .Citation {
        white-space: wrap;
        text-indent: 5rem hanging;
        color: black;

    }
    .active {
        animation-name: cite;
        animation-duration: 2s;
        animation-iteration-count: 2;
    }
    @keyframes cite {
        0%,
        50% {

            background-color: yellow;
        }
        100% {
            background-color: transparent;
        }
    }
    .Container {
        position: relative;
    }
    .cite-list {
        font-size: .8rem;
    }

</style>

